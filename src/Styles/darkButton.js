import Button from "@mui/material/Button";
import styled from "styled-components";

const DarkButton = styled(Button)`({  background: black;
    color: white;
    variant: contained;})

`;
export default DarkButton;
